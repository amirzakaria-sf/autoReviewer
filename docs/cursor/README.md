# Cursor folder

**Cursor writes here. Claude reads; Claude does not edit this folder.**

Protocol: [`../info.md`](../info.md). Read that at the start of every session, then
[`../STATUS.md`](../STATUS.md), then Claude's latest `../claude/changes.md` entries.

| File | Use |
|------|-----|
| `changes.md` | Append a block every session you change the repo. Newest first. |
| `notes.md` | Traps, "I already tried X", messages for Claude. |
| `architecture.md` | Scratch to merge into `../ARCHITECTURE.md`. Not source of truth. |
| `plans/` | Draft plans not yet accepted. |
| `specs/` | Draft specs not yet approved. |
